use mysql;

DELETE FROM user WHERE user='PMARLEN_DEVE';
GRANT SELECT,INSERT,UPDATE,DELETE ON PMARLEN_DB_DEVE.* TO 'PMARLEN_DEVE'@'localhost' IDENTIFIED BY 'PMARLEN_DEVE_PASSWORD';
GRANT SELECT,INSERT,UPDATE,DELETE ON PMARLEN_DB_DEVE.* TO 'PMARLEN_DEVE'@'%' IDENTIFIED BY 'PMARLEN_DEVE_PASSWORD';
